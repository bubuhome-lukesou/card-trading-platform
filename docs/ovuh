# H5 卡牌商城及拍卖平台开发文档

##1. 项目概述
- **目标**：构建一个基于 H5 的卡牌销售与拍卖平台，支持商户入驻、发布卡牌、查看竞拍进度，并及时通过微信通知买家/卖家。平台需包括前端展示、拍卖机制、支付/结算、微信推送、以及后台管理。
- **参考机制**：参考闲鱼拍卖，包括自动延时机制、出价阶梯、拍卖结束倒计时提醒等。拍卖过程需透明可追踪。
- **用户角色**：买家、卖家（个人/商家）、平台管理员。

##2. 功能流程

###2.1 用户端（H5 页面）
1. **入口页**：展示平台特色、活动卡牌、热门商户。
2. **卡牌浏览**：分类、搜索、筛选（稀有度、价格、商户）。
3. **卡牌详情**：图片轮播、属性描述、历史价格、当前竞拍状态（实时竞价+倒计时）、立即购买/出价按钮。
4. **出价流程**：
 - 用户需登录（微信授权登录）。
 - 可选择“立即购买”固定价或进入“拍卖”模式。
 - 拍卖出价需满足步进规则（例如每次至少比当前高5%）。
 - 倒计时：领先者出价时，若剩余时间少于阈值（如2 分钟），自动延时（如 +2 分钟）。
 - 拍卖结算：倒计时结束自动揭晓赢家，通知通过微信推送。
5. **订单/竞拍记录**：展示出价历史、订单状态、物流信息（若销售完成）。
6. **消息中心**：微信通知记录、平台内推送、客服入口。

###2.2 商家端（H5 或小程序/PC）
1. **商家认证**：资料审核、绑定支付/结算账户、上传资质。
2. **卡牌发布**：填写名称、图集、稀有度标签、起拍价、保留价、拍卖时间段、增价幅度、定价方式。
3. **库存管理**：已上架/待上架/已售数量，拥有卡牌 SKU 管理。
4. **拍卖数据**：监控在拍进行中的拍卖、出价人数、成交情况。
5. **报表导出**：销售业绩、佣金扣除、退款处理。

###2.3 后台管理
1. **权限体系**：管理员、审核员、客服、财务。
2. **审核系统**：
 - 商户入驻审核（材料、资质）。
 - 拍卖卡牌内容审核（确保合规）。
3. **拍卖监控**：实时流量、出价频率、异常竞拍（可疑行为提醒）。
4. **订单/结算管理**：订单列表、支付状态、退款、佣金设置、打款记录。
5. **数据分析**：交易趋势、热门卡牌、活跃用户数、转化率。
6. **微信通知管理**：模板/内容配置、发送日志、失败重发机制。
7. **系统设置**：拍卖规则调节、页面配置、主题营销（Banner、活动卡位）。

##3. 技术架构

###3.1 前端（H5）
- **框架**：Vue3 + Vite（兼容低版本浏览器的 polyfill），或 Taro/uni-app 跨端（如需快速适配小程序）。
- **UI组件**：使用自研组件库或 naive-ui/antdv/mobile版。设计需响应式，适配手机屏幕。
- **状态管理**：Pinia 或 Vuex（统一出价/订单状态）。
- **路由**：vue-router（前端路由 + 动态参数）。
- **数据请求**：Axios 封装请求拦截器（包含 token 注入、错误统一处理）。
- **实时竞拍**：WebSocket 或 Socket.IO 实现实时出价刷新 + 倒计时；备用方案为短轮询（1-3s）。
- **支付集成**：调用微信 JSAPI 支付（需服务端签名） +统一下单。
- **微信登录**：通过服务端获取微信登录链接 + code，并生成 session（可配合小程序/公众号）。
- **微信通知**：前端只触发成功/失败状态，实际通知由服务端调用公众号/订阅号模板消息。

###3.2 后端
- **语言/框架**：Node.js (NestJS/Express/Koa)、Python (Django/Flask)、或 Go（Gin）；以团队熟悉为准。建议使用 TypeScript + NestJS方便模块化。
- **数据库**：
 - MySQL/PostgreSQL：存订单、用户、拍卖卡牌、出价记录。
 - Redis：缓存拍卖数据、限流、实现出价延迟/延时逻辑。
- **实时组件**：
 - Socket 服务（如 Socket.IO + Redis适配多实例）。
 - Cron/定时任务负责拍卖结束判断（亦可用消息队列）。
- **存储**：卡牌图集采用 OSS（阿里云/腾讯云/七牛）+ CDN；生成水印/自动压缩。
- **支付/结算**：微信支付（商户号、API证书）、可集成 Stripe/PayPal 接入多个渠道。财务结算逻辑需支持分账/佣金。
- **微信通知**：调用微信模板消息（公众号/服务号）或订阅号，使用官方 SDK + 模板 ID + openid。需设计消息队列（RabbitMQ/Kafka）以确保高并发发送 + 重试。
- **安全**：
 - 黑名单/频率限制防止刷单；
 - 必须实现权限控制（基于 RBAC）；
 - 数据加密、访问日志。

###3.3 后台管理平台
- **前端**：Ant Design Pro / Vue Element Admin；统一管理后台页面。
- **接口认证**：使用 JWT + RBAC，刷新 token机制。
- **图表展示**：ECharts、BizCharts 显示交易趋势/拍卖热度。

###3.4 DevOps & 工程
- **代码仓库**：Git + GitHub/GitLab；采用分支策略（main/dev/feature）。
- **CI/CD**：GitHub Actions/GitLab CI，自动化构建、单测、部署（H5 发布到 CDN，后端容器化部署）。
- **监控/告警**：Prometheus + Grafana监控 API 延迟、任务队列；Sentry/LogRocket监控前端异常。
- **日志**：适配 ELK/腾讯云 CLS，记录支付回调、异常场景。
- **文档**：Swagger/OpenAPI 自动生成接口文档。

##4. 开发计划建议
1.需求确认 +竞品分析（1周）
2. 架构设计 + 原型（1周）
3. 基础功能开发（用户、商家、拍卖流程 + 后台）
4. 实时机制、支付、微信通知整合
5. 测试（功能、性能、安全）
6. 上线前准备（数据迁移、监控埋点、文档完善）

##5.需注意事项
- 拍卖延时逻辑：确保倒计时自动延时不泄漏数据；需集中在服务端判断，每次出价写入 Redis/DB 并触发倒计时刷新。
- 微信通知需提前申请模板，提供审批资料；确保消息内容可配置，避免频繁骚扰。
- 微信登录与支付需在同一域名下运行，配置微信 JS SDK 权限域。

如果这个结构没问题，我再细化每个模块的接口和数据库设计，或者立即搭建项目骨架。想先看哪部分？