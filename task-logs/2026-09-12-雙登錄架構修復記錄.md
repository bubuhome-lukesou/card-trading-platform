# 雙登錄架構修復 + 淘寶式登入界面 — 實施記錄 — 2026-09-12

## Luke 指示
修復雙登錄架構問題 + 結合淘寶登入界面方式

## 完成（commit 605f9d9c）

### 1. 淘寶式智能登入界面 ✅（前端）
- LoginView 移除「手機登入/電郵登入」雙 Tab → **單一「帳號」輸入框自動識別**：
  - 輸入含 `@` → 📧 電郵模式（顯示密碼欄）
  - 輸入數字 → 📱 手機模式（區號選單自動出現 + 驗證碼欄）
  - 模式提示：「📱 手機登入：將發送短信驗證碼」/「📧 電郵登入」
- 區號選單貼喺帳號框左邊（淘寶式，+853/+852/+886/+86）
- 手機輸入 computed getter 自動剝離已輸入嘅區號
- 空框提示：「輸入手機號或電郵，系統自動識別登入方式」
- autocomplete 屬性補齊（username/current-password）

### 2. email nullable 化 ✅（後端+DB）
- user entity：`email` 改 nullable（對齊淘寶/eBay/Amazon 主流做法）
- phoneLogin：手機註冊唔再造假 `phone_...@phone.card`，email 留 NULL、emailVerified=false
- JWT payload：email `|| null`
- DB 遷移（伺服器已執行）：
  - `ALTER TABLE users MODIFY email varchar(255) NULL` ✓
  - 清掉 1 個假 email → NULL ✓（remaining_fake=0 驗證）
  - `ALTER TABLE users ADD UNIQUE INDEX idx_users_phone (phone)` ✓（先查無重複數據）

### 3. 設置頁帳號顯示優化 ✅（#15 完成）
- Email 欄：空 →「未綁定（手機帳號）」；有值 → email + 「電郵為登入帳號，如需更改請聯繫客服」
- Phone 欄：掩碼顯示 `+853****315` + 「手機號可用於驗證碼登入，如需更改請聯繫客服」；未綁定顯示提示
- Phone 欄改 disabled（呢個係登入憑證，唔應該喺 profile PATCH 隨意改 — updateProfile 後端本來就只收 nickname/phone/avatar，phone 修改冇驗證流程係安全隱患）

## 驗證
- 前後端 build ✓ → push → 伺服器 pull + 遷移 + build + pm2 restart ✓
- email 登入實測 201 ✓、前端 200 ✓
- phone unique index 建立 ✓（Cardinality 3 = 3 個有手機嘅用戶）

## 日後可做（第三部分）
- 綁定郵箱：發驗證信確認流程（要 email 服務）
- 更換手機：短信驗證確認流程（SDK 已有，可做，約 2h）