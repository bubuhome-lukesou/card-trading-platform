# 三項修復記錄 — 品相/拍賣價格/拍賣頁統一 — 2026-09-12

## 完成（commit 53b33050）

### 1. 品相欄位格式統一 ✅
- seller/ProductsView.vue 商品品相欄位對齊其它選填項（productType/language）格式：
  - label「商品品相（選填）」→「商品品相」（同其它）
  - select 加 `class="form-select"`（同其它）
  - 「不指定」→「(請選擇)」（同 productType/language）
  - 加 field-hint：「選填 — 不選將顯示為「不指定」」
- 詳情頁 spec table 已有「不指定」顯示邏輯（condition 空 → —）

### 2. 拍賣添加報錯 price < 0.01 ✅
**根源：** CreateProductDto/UpdateProductDto 的 `price` 有 `@Min(0.01)` — 拍賣/預約模式下前端送 price=0（拍賣用 startingPrice 定價、預約用 deposit），被 DTO 擋。

**修復：**
- DTO：price 改 `@Min(0)`（Create + Update）
- service create()：按 listingType 驗證 — sale 必須 >0、auction/reservation price 可 0（不為負即可）

**E2E 驗證（seller token 實測）：**
- 拍賣 price=0 → 201 ✓（後建 auction record 201，currentPrice=100）
- 預約 price=0 → 201 ✓
- sale price=0 → 400「售價必須大於 0」✓（正確行為）
- 測試商品已清理

### 3. 拍賣數量固定 1 ✅
- ProductsView 數量欄位：拍賣模式時 `disabled` + 提示「拍賣商品數量固定為 1」
- watch listingType 切入 auction 時強制 quantity=1

### 4. 拍賣頁圖片/資訊展示統一 ✅
AuctionDetailView 對齊 ProductDetailView：
- 圖片容器：白底 cover → **深底漸變（#131318→#0e0e14）+ contain 顯示**（唔會裁卡）
- 加 **cat-pill**（分類 emoji+名稱浮動左上，同商品詳情頁）
- 加 **thumb-strip 縮圖條**（76px 方形縮圖、active 高亮、hover 上浮）
- hero-img hover 微放大（scale 1.03）
- 保留拍賣頁特有：收藏按鈕、lightbox、左右切換、圖片計數

## 部署驗證
前後端 build ✓ → push → 伺服器 pull + build + pm2 restart ✓ → API E2E 全通 ✓